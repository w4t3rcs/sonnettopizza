server:
  port:
    8080

spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id: user-service
            uri: lb://user-service
            predicates:
              - Path=/api/v1.0/users
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/