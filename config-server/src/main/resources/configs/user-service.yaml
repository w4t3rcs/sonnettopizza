server:
  port: 8081
spring:
  datasource:
    username: user
    password: password
    url: jdbc:mysql://mysql:3306
    name: userdb
    hikari:
      initialization-fail-timeout: 0
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: *
  health:
    show_details: always