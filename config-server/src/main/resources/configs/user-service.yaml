server:
  port: 8081
spring:
  datasource:
    username: user
    password: password
    url: jdbc:mysql://sonnetto-mysql:3306/userdb?useSSL=false&createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/